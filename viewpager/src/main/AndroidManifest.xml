<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hsf.viewpager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackTest">

        <activity android:name=".InitActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".normal.MainActivity"
            android:exported="true">
        </activity>

        <activity android:name=".withtab.WithTabActivity"/>

        <activity android:name=".radio_group.RadioGroupActivity"/>

        <activity android:name=".bottom.BottomNavigationActivity"/>
    </application>

</manifest>