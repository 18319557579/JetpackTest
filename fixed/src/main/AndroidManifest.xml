<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hsf.fixed">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackTest">
        <activity android:name=".item_decoration_two.ItemDecorationTwoActivity"></activity>
        <activity android:name=".item_decoration.ItemDecorationActivity" />
        <activity android:name=".use_material.UseMaterialActivity" />
        <activity android:name=".one_view.OneViewActivity" />
        <activity android:name=".header_footer.HeaderFooterActivity" />
        <activity android:name=".two_same.TwoSameTopBarActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>